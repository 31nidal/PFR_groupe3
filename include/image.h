#ifndef IMAGE_H
#define IMAGE_H

/* Convertir un nombre décimal en binaire (stocké dans une chaîne) */
char* decimalEnBinaire(int decimal);

/* Convertir un nombre binaire en décimal */
int binaireEnDecimal(char* binaire);

#endif
